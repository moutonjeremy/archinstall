# Arch Linux Packages List
# Lines starting with # are comments
# Format:
#   package-name        -> pacman package
#   [aur] package-name  -> AUR package
#   [cmd] command       -> custom shell command

# Terminal and shell
alacritty
zsh

# Wayland compositor
niri
sddm
fuzzel
waybar

# Browser
firefox

# Custom commands (examples)
[cmd] sudo systemctl enable sddm
[cmd] chsh -s /usr/bin/zsh
